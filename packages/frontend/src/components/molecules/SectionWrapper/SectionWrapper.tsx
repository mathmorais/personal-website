import { SectionWrapperContainer } from "./SectionWrapper.styles";

export const SectionWrapper: React.FC = ({ children }) => {
	return <SectionWrapperContainer>{children}</SectionWrapperContainer>;
};
